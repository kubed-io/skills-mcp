apiVersion: v1
kind: Service
metadata:
  name: skills-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: skills-mcp
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: http
